package rn.standard.sorting;

public class QuicksortSorter {
    public void sort(int[] values) {
        int startIndex = 0;
        int endIndex = values.length - 1;
        int partIndex = 0;
        int partition = values[partIndex];

        while (startIndex < endIndex) {

        }
    }
}
